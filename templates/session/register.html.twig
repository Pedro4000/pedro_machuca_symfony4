{% extends 'base.html.twig' %}

{% block title %}Hello RegistrationController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
<div class="registration-section">
    <h1>Bonjour, veuillez rentrer vos informations perso</h1>
    <div class='registration-form'>
    	<p class='info-intro'>Créez votre compte Pedro Machuca</p>
    	{{ form_start(registrationForm)}}
	    	<div >
	        {{ form_widget(registrationForm.firstName) }}
	        {{ form_widget(registrationForm.lastName) }}
	        <div>
    	{{ form_widget(registrationForm.email)}}
        {{ form_widget(registrationForm.password) }}
		<button type='submit' class='submit-registation'>M'inscrire</button>
        {{ form_end(registrationForm) }}
</div>
{% endblock %}
